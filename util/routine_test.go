package util

/*
if close != nil {
   select {
   case <-stopTicker.C:
       select {
       case <-close.C:
           LogDebug("%v", "exchange-routine : closed")
           return
       default:
       }
   default:
   }
}

*/
